import React from'react';import{Clock,Star,Plus}from'lucide-react';import{useCart}from'../context/CartContext';import{formatCurrency}from'../utils/formatters';export default function RestaurantCard({item}){const{addToCart}=useCart();return <article className="card restaurant"><div className="visual foodvisual">{item.emoji}</div><div className="cardbody"><div className="row"><h3>{item.name}</h3><span className="rating"><Star size={13} fill="currentColor"/>{item.rating}</span></div><p>{item.cuisine}</p><div className="meta"><span><Clock size={14}/>{item.time}</span><span>{formatCurrency(item.price)}</span></div><button className="smallbtn" onClick={()=>addToCart({id:`food-${item.id}`,name:item.name,price:item.price,emoji:item.emoji})}><Plus size={15}/>Add meal</button></div></article>}
