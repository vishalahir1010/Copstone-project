import React from'react';import{Play,Pause}from'lucide-react';import{useMusic}from'../context/MusicContext';export default function SongRow({song,index}){const{currentSong,isPlaying,playSong,togglePlay}=useMusic();const active=currentSong?.id===song.id;return <div className={active?'songrow active':'songrow'}><span className="num">{index+1}</span><div className="album">{song.emoji}</div><div className="songinfo"><b>{song.title}</b><span>{song.artist} • {song.album}</span></div><span>{Math.floor(song.duration/60)}:{String(song.duration%60).padStart(2,'0')}</span><button className="playmini" onClick={()=>active?togglePlay():playSong(song)}>{active&&isPlaying?<Pause size={15}/>:<Play size={15}/>}</button></div>}
