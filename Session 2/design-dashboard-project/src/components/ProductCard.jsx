import React from'react';import{Plus}from'lucide-react';import{useCart}from'../context/CartContext';import{formatCurrency}from'../utils/formatters';export default function ProductCard({product}){const{addToCart}=useCart();return <article className="card product"><div className="productvisual">{product.emoji}</div><div className="cardbody"><small>{product.category}</small><h3>{product.name}</h3><div className="price"><b>{formatCurrency(product.price)}</b><del>{formatCurrency(product.oldPrice)}</del></div><button className="primary smallbtn" onClick={()=>addToCart(product)}><Plus size={15}/>Add to cart</button></div></article>}
